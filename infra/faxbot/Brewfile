# Faxbot - Homebrew Bundle
# Install with: brew bundle

# Core dependencies
brew "node"
brew "git"
brew "docker"
brew "docker-compose"

# Optional but recommended
brew "curl"
brew "wget"

# Development tools (optional)
brew "nodemon"
