# =============================================
# FaxBella (faxbella.com) - Environment Variables
# =============================================
# Copy this file to .env.local for local development.
# Production secrets are set in the Convex Dashboard, NOT here.

# ─── Client-Side (committed to .env.local) ──────────────────

# Convex deployment URL (from Convex Dashboard > Settings)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Stripe publishable key (safe for client)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# ─── Server-Side: Convex Environment Variables ──────────────
# Set these in Convex Dashboard > Settings > Environment Variables
# They are available inside Convex functions (actions, mutations, etc.)

# HumbleFax - Fax sending and receiving
# HUMBLEFAX_ACCESS_KEY=your_access_key
# HUMBLEFAX_SECRET_KEY=your_secret_key

# Google Gemini AI - OCR, document classification, routing
# GEMINI_API_KEY=your_gemini_api_key

# EmailIt - SOLE email provider (NEVER use Resend/SendGrid/Postmark)
# EMAILIT_API_KEY=your_emailit_api_key

# Stripe - Primary payment processor
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Whop - Backup payment processor (admin-toggleable)
# WHOP_API_KEY=your_whop_api_key
# WHOP_WEBHOOK_SECRET=your_whop_webhook_secret
# WHOP_COMPANY_ID=your_whop_company_id

# ─── Cloudflare (set in Cloudflare Dashboard) ───────────────
# These are set in Cloudflare Pages > Settings > Environment Variables
# ENVIRONMENT=production

# ─── WebAuthn / Passkey Auth ────────────────────────────────
# No additional env vars needed. Passkey auth uses Convex DB
# for challenge/credential/session storage. The relying party ID
# is hardcoded to "faxbella.com" in convex/passkeys.ts.

# =============================================
# EMAIL POLICY: EmailIt ONLY
# =============================================
# All email notifications MUST be sent via EmailIt.
# NEVER use Resend, SendGrid, Postmark, or any other email provider.
# This includes:
#   - Fax delivery notifications
#   - Confirmation emails
#   - Transactional emails
#   - All user communications
#
# =============================================
# PAYMENT POLICY: Stripe (primary) + Whop (backup)
# =============================================
# Stripe is the default payment processor.
# Whop is available as a backup, controlled by an admin
# toggle stored in the paymentSettings Convex table.
# Both processors must remain in the codebase.
